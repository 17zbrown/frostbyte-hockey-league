# Chel Gaming gateway bot — copy to /etc/chel-bot.env on the VM and fill in real values.
# These are the SAME four values the Netlify functions use: Netlify dashboard →
# Site configuration → Environment variables. Never commit the filled-in file.
DISCORD_BOT_TOKEN=
DISCORD_GUILD_ID=
SUPABASE_URL=https://bzbuyclwdhmhdzujxeqd.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
